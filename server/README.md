# Local API

Express API for the local HDT Battlegrounds Tracker database.

```powershell
corepack pnpm --filter server seed
corepack pnpm --filter server dev
```

Set `HDT_BG_TRACKER_DATA` to point at a different data directory during development or tests.

